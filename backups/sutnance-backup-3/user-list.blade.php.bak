<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">User List</h2>
    </x-slot>

    <style>
        :root {
            --SEAAL-primary: #003366;
            --SEAAL-primary-light: #004488;
            --SEAAL-green: #00A859;
            --SEAAL-green-light: #00C96D;
            --SEAAL-gray-bg: #F8F9FA;
            --SEAAL-gray-border: #E9ECEF;
            --SEAAL-gray-text: #6C757D;
        }
        
        .SEAAL-bg-primary { background-color: var(--SEAAL-primary); }
        .SEAAL-bg-primary-light { background-color: var(--SEAAL-primary-light); }
        .SEAAL-bg-green { background-color: var(--SEAAL-green); }
        .SEAAL-bg-green-light { background-color: var(--SEAAL-green-light); }
        .SEAAL-bg-gray { background-color: var(--SEAAL-gray-bg); }
        
        .SEAAL-text-primary { color: var(--SEAAL-primary); }
        .SEAAL-text-green { color: var(--SEAAL-green); }
        .SEAAL-text-gray { color: var(--SEAAL-gray-text); }
        
        .SEAAL-border { border-color: var(--SEAAL-gray-border); }
        .SEAAL-border-primary { border-color: var(--SEAAL-primary); }
        .SEAAL-border-green { border-color: var(--SEAAL-green); }
        
        .SEAAL-ring-primary:focus { ring-color: var(--SEAAL-primary); }
        .SEAAL-ring-green:focus { ring-color: var(--SEAAL-green); }
        
        .SEAAL-shadow { box-shadow: 0 2px 8px rgba(0, 51, 102, 0.08); }
        .SEAAL-shadow-lg { box-shadow: 0 4px 16px rgba(0, 51, 102, 0.12); }
    </style>

    <div class="flex min-h-screen bg-gray-50">
        <x-sidebar />

        <div class="flex-1 SEAAL-bg-gray p-8 pl-12">
            <div class="max-w-5xl">
                @if(session('success'))
                    <div class="mb-4 p-4 bg-green-100 text-green-800 rounded">{{ session('success') }}</div>
                @endif
                @if(session('error'))
                    <div class="mb-4 p-4 bg-red-100 text-red-800 rounded">{{ session('error') }}</div>
                @endif

                <div class="w-full">
                    <div class="bg-white rounded-xl p-6 SEAAL-shadow">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-lg font-bold SEAAL-text-primary">Tous les utilisateurs</h3>
                            @if(auth()->user() && auth()->user()->role === 'admin')
                                <a href="{{ route('users.create') }}" class="px-4 py-2 SEAAL-bg-green text-white rounded hover:SEAAL-bg-green-light">Ajouter un utilisateur</a>
                            @endif
                        </div>

                        <div class="overflow-x-auto">
                            <table class="w-full text-left">
                                <thead>
                                    <tr class="text-sm text-gray-500 border-b">
                                        <th class="py-3">Avatar</th>
                                        <th class="py-3">Nom</th>
                                        <th class="py-3">E-mail</th>
                                        <th class="py-3">Rôle</th>
                                        <th class="py-3">Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach($users as $u)
                                        <tr class="border-b">
                                            <td class="py-3">
                                                @if($u->profile_photo)
                                                    <img src="{{ asset('storage/' . $u->profile_photo) }}" class="rounded-full object-cover" alt="avatar" style="width:40px;height:40px;object-fit:cover;border-radius:9999px;">
                                                @else
                                                    <div class="rounded-full bg-gray-200 flex items-center justify-center text-sm text-gray-500" style="width:40px;height:40px;border-radius:9999px;">N/A</div>
                                                @endif
                                            </td>
                                            <td class="py-3">{{ $u->name }}</td>
                                            <td class="py-3">{{ $u->email }}</td>
                                            <td class="py-3">{{ $u->role }}</td>
                                            <td class="py-3">
                                                @if(auth()->user() && auth()->user()->role === 'admin')
                                                    <div class="flex items-center gap-2">
                                                        <a href="{{ route('users.edit', $u) }}" class="inline-block px-3 py-1 SEAAL-bg-primary text-white rounded hover:SEAAL-bg-primary-light">Modifier</a>
                                                        @if(auth()->id() !== $u->id)
                                                            <form method="POST" action="{{ route('users.destroy', $u) }}" onsubmit="return confirm('Supprimer cet utilisateur ?');" class="inline">
                                                                @csrf
                                                                @method('DELETE')
                                                                <button type="submit" class="px-3 py-1 bg-red-600 text-white rounded hover:bg-red-700">Supprimer</button>
                                                            </form>
                                                        @else
                                                            <span class="text-sm text-gray-500">(Vous)</span>
                                                        @endif
                                                    </div>
                                                @else
                                                    @if(auth()->id() === $u->id)
                                                        <span class="text-sm text-gray-500">(Vous)</span>
                                                    @endif
                                                @endif
                                            </td>
                                        </tr>
                                    @endforeach
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
</x-app-layout>
